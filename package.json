{
  "name": "bookstore",
  "version": "1.0.0",
  "description": "Full-stack single page application for a bookstore. The frontend is built with React & Typescript and the backend is built with Node.js and TypeScript.",
  "scripts": {
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
